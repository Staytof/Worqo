import type { ChatThread, Post, ServicePin, WorkerProfile } from "../types";

export const seedPins: ServicePin[] = [];

export const seedPosts: Post[] = [];

export const seedChats: ChatThread[] = [];

export const seedWorkers: WorkerProfile[] = [];
